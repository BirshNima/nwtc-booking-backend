{
  "name": "nwtc-booking-backend",
  "private": true,
  "version": "1.0.0",
  "description": "Serverless backend (Netlify Functions) for the Northwest Town Car Service booking funnel — intake, deposit links, cash reconciliation, Stripe webhook.",
  "dependencies": {
    "stripe": "^17.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
